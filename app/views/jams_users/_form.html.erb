<%= simple_form_for(@jams_user) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :jam %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
